<map id="myMap" show-location bindtap="handmap" callout="{{callinfo}}" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" controls="{{controls}}" bindcallouttap="markertap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}"
  bindregionchange="regionchange" style="width: 100%; height: {{windowsHeight}}px;">
  <cover-view>
<mp-icon class="left" type="field" icon="location" color="black" size="{{iconSize}}"></mp-icon>
  </cover-view>

</map>